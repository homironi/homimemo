{% macro card(post) %}
    <div class="card">
        <a href ="{{ post.url }}" class ="cardInner">
            <img 
            src="{{ post.data.headerImg }}" 
            alt= "{{ post.data.headerImgAlt }}"
            /> 
            <div class ="cardContent">
                <h3 class = "title">{{ post.data.title }}</h3>
                <p class ="description">{{ post.data.description }}</p>
            </div>
        </a>
    </div>
{% endmacro %}