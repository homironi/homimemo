---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a">{};

if(!Astro.slots.has("default")){
  throw Error("子要素は必須です。子要素がありません");
}
---

<p>TODO：ExternalLink <slot/></p>
